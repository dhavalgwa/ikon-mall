import { StyleSheet } from 'react-native';

const Styles = StyleSheet.create({
    searchView: {
        flex: 1
    },
    bannerView: {
        flex: 1, marginTop: 20, paddingLeft: 10
    }
})

export default Styles;