import { StyleSheet } from 'react-native';

const Styles = StyleSheet.create({
    NotificationCard: {
        backgroundColor: 'white', justifyContent: 'space-between', margin: 10, flexDirection: 'row'
    }
})

export default Styles;